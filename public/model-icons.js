import {MODEL_IDENTITIES,MODEL_BRANDS} from './model-identities.js';
const escape=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const aliases={'Google':'google','Meta':'meta-llama','Meta AI':'meta-llama','Mistral AI':'mistralai','Microsoft':'microsoft','DeepSeek':'deepseek-ai','OpenAI':'openai','Moonshot AI':'moonshotai','Hugging Face':'HuggingFaceTB','Qwen':'Qwen','NVIDIA':'nvidia'};
export function modelIdentity(model){const owner=MODEL_IDENTITIES[model?.id]||model?.base_model?.split('/')[0]||aliases[model?.author]||model?.repo?.split('/')[0]||model?.files?.[0]?.repo?.split('/')[0]||model?.author||model?.family||'LocalNeuron';const brand=MODEL_BRANDS[owner]||{name:owner};const words=owner.replace(/[^\p{L}\p{N}]+/gu,' ').trim().split(/\s+/);const initials=(words.length>1?words[0][0]+words.at(-1)[0]:words[0]?.slice(0,2)||'AI').toUpperCase();let hash=0;for(const c of owner)hash=(hash*31+c.charCodeAt(0))>>>0;return {owner,...brand,initials,tint:hash%8};}
export function modelIcon(model){const b=modelIdentity(model);return `<span class="model-logo ${b.file?'':'logo-fallback identity-'+b.tint}" title="${escape(b.owner)}"><span ${b.file?'hidden':''} aria-label="Identificação de ${escape(b.owner)}">${escape(b.initials)}</span>${b.file?`<img src="/model-icons/${escape(b.file)}" alt="Ícone de ${escape(b.owner)}" loading="lazy" decoding="async">`:''}</span>`;}
if(typeof document!=='undefined')document.addEventListener('error',e=>{const img=e.target;if(img?.matches?.('.model-logo img')){const parent=img.parentElement;img.remove();parent.classList.add('logo-fallback');parent.querySelector('span').hidden=false;}},true);
